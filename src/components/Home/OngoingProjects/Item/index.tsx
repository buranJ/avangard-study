import { FC, useEffect, useRef, useState } from 'react';
import { useNavigate } from 'react-router-dom';

import { IObjectsList } from 'types/common';

import './style.scss';

const Item: FC<IObjectsList> = ({ title, info, imagepng, imagebg, is_new, mini_title, slug }) => {

  return (
   
  );
};

export default Item;

