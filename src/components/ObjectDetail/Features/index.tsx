import { FC } from 'react';

import './style.scss';

interface IProps {
  advantages: {
    title: string;
    iconUrl: string;
    id: number;
  }[];
}

const Features: FC<IProps> = ({ advantages }) => {
  return (
    <>
    </>
  );
};

export default Features;

